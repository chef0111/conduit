{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["generate", "^build"]
    },
    "typecheck": {
      "dependsOn": ["generate", "^typecheck", "^build"]
    },
    "generate": {
      "outputs": []
    }
  }
}
